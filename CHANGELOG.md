# 변경 이력 (CHANGELOG)

> AI Home Layout Debugger — 개발 진행 기록
> 컨셉: 사람이 배치하고, AI가 검증·분석·해결안을 제안한다 (가구 배치의 Grammarly)

---

## 공개 배포 사전 준비 · 2026-09-20

- **자원 준비 완료·코드 미배포·서비스 미검증**: 사용자 크레딧 확인 및 비용 승인 후 Japan East Linux Basic B1 인스턴스 1개 생성. Korea Central은 학생 정책으로 거부되어 지역 변경을 승인받음. Python 3.12/Always On/HTTPS/TLS 1.2/FTP 비활성화, Gemini 키는 사용자가 포털에서 직접 등록.
- 별도 일반 프롬프트로 지정 Gemini 모델의 연결 성공 확인(`OK` 응답). 애플리케이션 배포/통합 검증과는 구분하며 영속 파일은 임시 실행 디렉터리가 아닌 `/home/layout-data/` 사용.
- 무료 전용 `gemini-3.5-flash-lite` 경로 및 명시적 `LLM_PROVIDER` 선택 추가. 공개 설정에서는 다른 모델·유료 제공자·CLI·템플릿 폴백 금지.
- 앱 전체 실제 AI 호출 10회/60초·400회/24시간·동시 1회. 검증 재시도와 실패 호출도 차감.
- 호출 전 파일 잠금·원자적 저장·fsync로 사용량을 영속화. 손상/쓰기 실패/시계 역행은 명시적으로 차단하며 SQLite/WAL을 사용하지 않음.
- `/api/health`, 외부 `SAVED_LAYOUT_PATH`, Azure `/home` 설정 문서, 시작 시 설정·저장소 검사.
- AI 입력·대화·프롬프트·응답과 공개 씬 크기 제한, HTTP 상태/오류 코드/Retry-After, 한국어/영어 UI 오류 표시.
- 공용 씬·민감정보 입력 금지·무료 Gemini 데이터 사용·공용 한도를 UI에 고지.
- 공개 설정과 이전 로컬 동작을 분리하며 결정론적 기하/검증/점수 알고리즘은 변경하지 않음.
- 제공자·재시도·재시작·동시성·한도·입력·UI 회귀 테스트 추가. **클라우드 배포 완료를 주장하지 않는 사전 준비 변경**.
- 검증: 기존 80개를 포함한 pytest **142개 통과**, Node UI 오류/크기 검사 포함. 실제 HTTP 기본 9건→자동 수정 0건·100점 (0.546초).

## 검증·국제화·배치 엔진 개선 · 2026-09-20

- Wanted 디자인 참고 자료를 반영한 밝은 반응형 UI, 파란 액센트, 정돈된 카드/입력/3D 배경/리포트.
- 데스크톱 및 375~1440px 화면 폭 브라우저 검증, 실제 앱 예시 화면 제공.
- 시작 시 저장본 존재 확인 누락을 수정하고, 언어 전환·저장·오류 복구에 대한 복원 버튼 회귀 검사 추가.
- 최종 검증: pytest 80개 통과, 실제 HTTP 자동 수정 9건 → 0건·100점 약 0.66초.
- 자동 수정 활동 목록에서도 위반 코드 대신 선택 언어의 읽기 쉬운 유형명을 표시.
- README에 한국어 검사/자동 수정/English/리포트의 실제 1440×1000 화면 4장 삽입.
- 기본 데모의 코드·대상 쌍·측정값과 각 1순위 해결안을 고정하는 pytest 기준선 추가.
- 한국어/영어 전체 UI, API 설명, AI 응답 언어 선택 및 출력 escape. 한글 이름과 조사 헬퍼.
- 원본과 분리한 원자적 저장, 시작 시 저장본 로드, Undo 가능한 저장본 복원/데모 초기화.
- 부족량을 반영한 제한 감점과 위반별 penalty, 카드·검토 리포트 설명.
- `USAGE_SPACE`: 회전 방향의 앞 공간, 벽·건물 외곽, 침대 한 측면 규칙, 냉장고·세탁기 600mm.
- 기본 책상을 180°로 수정. 기존 6건을 유지하고 동선 3건을 추가해 기준선 9건·0점.
- NumPy 100mm 격자, 600mm 폭 BFS, 가구 제거 기반 막는 대상 추론, 바닥 경로 표시.
- AI 명령은 일괄 사전 검증 후 적용. 새 위반 시 1회 재시도, 동일 작업만 명시적으로 강제 적용.
- 엔진 주도 `place`: 방 안 100mm × 4방향 기하 필터와 최대 64개 전체 검사.
- 구조물 이동 후보 제거, 대각선·회전+이동 확장, 새 위반 유발 후보 제거.
- 후보의 빠른 기하 필터 도입 후 전체 해결안 조회 약 1.06초, 자동 수정 약 0.61초(측정 환경 기준).
- `Furniture`/`Walkway`와 새 Python 필드명 도입. 옛 모델명·JSON 키 호환 유지.
- 면 접촉만 있는 깊이 0의 `HARD_CLASH` 오검출 수정.
- 실행·규칙·구조·테스트·호환성·API·제한사항 문서 갱신.

## 가구 회전 + 배치 점수/검토 리포트 · 2026-09-17

**가구 90° 회전**
- Properties 패널 `↻ 90° 회전` 버튼 + `R` 단축키 — AABB 가로/세로 스왑 + rotation 상태(0/90/180/270) 저장, 3D 모델이 실제로 회전 (옷장 문 방향 등 반영)
- **해결안 생성기에 회전 후보 추가**: "가구 X를 제자리에서 90° 회전" — 이동보다 저비용이라 검증 통과 시 우선 추천
- AI Copilot에 rotate op 추가 ("옷장 돌려줘")

**배치 점수 + 검토 리포트**
- 결정론적 점수: 100 − (HIGH×15) − (MEDIUM×7) — 통계 패널에 실시간 표시, 편집 즉시 반영
- 위반 0건 시 "🏆 배치 승인 가능" 배너
- `📄 검토 리포트`: 인앱 모달 — 점수/검사/통과/위반 카드, 잔여 위반 테이블, **세션 수정 이력**(수동 편집·해결안 적용·AI 자동 수정·AI 명령 타임라인), 인쇄/PDF 저장
- 백엔드: 모든 변경 API에 이력 로깅, `GET /api/report`

## 절차적 가구 3D 모델 · 2026-09-17

- 단색 박스 → 가구별 조립 형상: 침대(프레임+매트리스+이불+베개+헤드보드), 옷장(양문+손잡이), 책상/테이블(상판+다리 4개), 소파(좌판+등받이+팔걸이+쿠션), 냉장고(도어 분할선+손잡이), 책장(측판+선반 3단), TV장(캐비닛+스크린), 세탁기(드럼 도어+컨트롤 패널)
- 충돌 판정은 기존 AABB 그대로 — 형상은 박스 내부에만 렌더링, 희미한 충돌 박스 외곽선 유지
- 선택/하이라이트/이동 기즈모가 그룹 단위로 동작 (모든 부품이 함께 빨간 강조·이동)

## 방 구역(Room) 도입 + 동선 제거 · 2026-09-14

- **동선(walkway) 기능 제거** — 데이터·툴바 버튼·AI 프롬프트에서 삭제 (백엔드 지원은 유지, 씬에서 미사용)
- **rooms 개념 도입**: 거실·주방 / 침실 / 서재가 각각 독립된 직사각형 구역 — 색상 바닥 + 테두리 + 이름 라벨로 시각 구분
- **신규 위반 OUT_OF_ROOM (HIGH)**: 가구가 방 하나에 완전히 들어가지 않으면(벽에 걸치거나 방 사이에 끼면) 검출
- 벽/바닥의 3D 라벨 제거로 화면 정리
- 씬: 152건 검사 → 위반 6건 (소파↔테이블 겹침, TV장↔벽 관통, TV장 방 이탈, 침대↔창문, 옷장↔침실문, 책장↔책상)
- Auto-Fix: TV장 1회 이동으로 벽 관통+방 이탈 동시 해소 포함, **6건 → 0건 (2.4초, 수정 5회)**

## 주택 전체로 확장 (거실·침실·서재) · 2026-09-14

- 원룸 1칸 → **9.0m x 6.0m 1인 주택 3개 공간** (거실+주방 / 침실 / 서재)
- **내벽(wall) 구조 타입** 추가: 문 개구부(0.9~1.1m)를 사이에 둔 벽 세그먼트 4개 — 가구가 벽을 관통하면 HARD_CLASH
- 방 사이 문 3개(현관/침실/서재)의 개폐 구역 + 창문 2개 확보 구역
- 동선 2개: 현관→침실(침실문 통과), 현관→서재(서재문 통과) — 문 개구부를 지나는 경로가 벽 세그먼트와의 이격까지 검증됨
- 가구 9개 배치, 의도된 위반 5건: 소파↔동선 관통, TV장↔내벽 관통, 침대↔창문 구역, 옷장↔침실문 구역, 책장↔책상 사용공간
- **검증: 171건 검사 → 5건 정확 검출 → Auto-Fix 9.2초에 0건** (소파 이동이 유발한 2차 간섭까지 연쇄 해결)
- 책상 사용공간 검사를 가구 대상으로 한정 (책상은 원래 벽에 붙이므로 벽은 제외)

## 초기 릴리스 · 2026-09-14

**도메인 이식 (선박 → 주거)**
- [K-Shipbuilding-AI-Hackathon](https://github.com/GeunheePARKKK/K-Shipbuilding-AI-Hackathon)의 도메인 독립 코어(기하 엔진, Rule Engine, Resolver, Auto-Fix Agent, LLM 레이어, 3D 편집기)를 그대로 재사용
- 기관실→원룸, 장비→가구, 배관→동선(walkway), 이격거리→인체공학 기준으로 어휘·데이터·프롬프트 전환

**주거 특화 신규 기능**
- `zone` 구조 타입: 문 개폐 구역·창문 앞 확보 구역 — 반투명 렌더링, 가구 침범 시 `ZONE_INTRUSION`(MEDIUM) 전용 위반
- 동선(walkway): 폭을 가진 통행 경로. 가구가 침범하면 HARD_CLASH, 동선끼리 교차는 허용
- 바닥 평면 제약: 가구·동선의 Z축 이동 제거 (해결안 생성기 + 3D 기즈모 모두)
- zone은 이동 불가 (창문·문은 건물 고정) — 해결안 후보에서 제외
- Auto-Fix Agent 진동 방지 가드: 한 번 고친 위반이 재발하면 재시도 대신 "수동 검토 필요"로 표시

**검증 결과**
- 원룸 데모 씬(가구 6개, 동선 1개, 구역 2개): 47건 검사 → 의도된 위반 4건 정확 검출
  1. 소파 ↔ 주 동선 침범 (HARD_CLASH, 300mm 관통)
  2. 침대 ↔ 창문 앞 구역 200mm 침범
  3. 옷장 ↔ 현관문 개폐 구역 550mm 침범
  4. 책장 ↔ 책상 사용 공간 (400mm < 750mm)
- 전체 자동 수정: 4건 → 0건, 2.7초

**지식 베이스**
- 인체공학 규칙 6건 (동선 폭 600/900, 문 개폐, 창 앞 500, 책상 750, 옷장 600, 침대 측면 300)
- 원룸 배치 사례 4건 (RAG-lite 태그 매칭)
